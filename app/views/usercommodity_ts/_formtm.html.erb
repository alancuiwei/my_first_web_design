<%= form_for(@usercommodity_t) do |f| %>
  <% if @usercommodity_t.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usercommodity_t.errors.count, "error") %> prohibited this usercommodity_t from being saved:</h2>

      <ul>
      <% @usercommodity_t.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
     <br />
    商品品种“<%= @usercommodity_t.commodityid%>”保证金比例修改：<br />
    <%= f.text_field :trademargingap ,:value=>@exchtrademargin[@usernum].to_d+@usercommodity_t.trademargingap.to_d %>
    <% session[:exchtrademargin]=@exchtrademargin[@usernum].to_d  %>
  </div>
  <div class="actions">
    <%= f.submit :"提交" %>
  </div>
<% end %>
