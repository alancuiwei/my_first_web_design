
<%= form_for(@noriskmessage) do |f| %>
  <% if @noriskmessage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@noriskmessage.errors.count, "error") %> prohibited this noriskmessage from being saved:</h2>

      <ul>
      <% @noriskmessage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
  </div>
  <div class="field">
    <%= f.label :标题： %><br />
    <%= f.text_field :title %>
  </div><br>
  <div class="field">
    <%= f.label :内容： %><br />
    <%= f.text_area :body ,:rows=>"5" ,:cols=>"40" %>
  </div><br>
  <div class="actions">
    <%= f.submit :value=>"发表评论"%>
  </div>
<% end %>
